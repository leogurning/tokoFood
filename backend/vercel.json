{
    "version": 2,
    "buildCommand": "npm run build",
    "builds": [{
        "src": "./built/index.js", 
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "./built/"
    }]
}